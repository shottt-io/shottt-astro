# مستندات سیستم طراحی (Design System Guidelines)

به منظور حفظ یکپارچگی بصری، ارتقای تجربه کاربری (UX) و مدرن‌سازی رابط کاربری پلتفرم شات، دستورالعمل‌های زیر در تمامی بخش‌های برنامه باید رعایت شوند:

## تاییدها و دیالوگ‌ها (Confirmations & Dialogs)

### عدم استفاده از متدهای بومی مرورگر (Deprecation of Browser Native Prompts)
استفاده از متدهای بومی مرورگر مانند `confirm()`، `alert()` و `prompt()` برای کارهای مدیریتی و تاییدهای حیاتی در تمام صفحات (به ویژه پنل مدیریت) **ممنوع** است. این ابزارها با طراحی اختصاصی و پرمیوم پلتفرم همخوانی ندارند.

### راهکار جایگزین: دیالوگ‌های سفارشی (Custom `<dialog>` Modals)
تمامی تاییدها و اعلان‌ها باید با استفاده از دیالوگ‌های سفارشی و با بهره‌گیری از ویژگی استاندارد HTML `<dialog>` بازنویسی شوند. بدین منظور دو متد سراسری بر روی آبجکت `window` تعریف شده است:
1. `window.showConfirmDialog(message, options)`: جهت دریافت تایید (بله/خیر) از کاربر.
2. `window.showAlertDialog(message, options)`: جهت نمایش پیام یا هشدار تک‌دکمه‌ای.

#### نمونه کد نحوه استفاده:

```javascript
// ۱. تایید حذف با ظاهر خطر (Rose/Danger)
const confirmed = await window.showConfirmDialog('آیا از حذف این مورد مطمئن هستید؟', {
  okText: 'حذف شود',
  cancelText: 'انصراف',
  type: 'warning' // حالت پیش‌فرض تایید
});

if (confirmed) {
  // عملیات حذف...
}

// ۲. نمایش پیام موفقیت‌آمیز (Success)
await window.showAlertDialog('تیکت شما با موفقیت ثبت شد.', {
  type: 'success', // گزینه‌ها: 'success' | 'error' | 'info' | 'warning'
  okText: 'باشه'
});

// ۳. نمایش پیام خطا (Error)
await window.showAlertDialog('خطا در ارتباط با سرور. لطفا مجدداً تلاش کنید.', {
  type: 'error'
});
```

### ویژگی‌های بصری دیالوگ سفارشی
1. **پس‌زمینه مات (Backdrop Blur):** استفاده از `backdrop-filter: blur(8px)` به همراه یک رنگ تیره شفاف جهت جداسازی کامل دیالوگ از صفحه زمینه.
2. **انیمیشن ورودی نرم (Entrance Animation):** دیالوگ با انیمیشن Scale و حرکتی ملایم به سمت بالا ظاهر می‌شود.
3. **تایپوگرافی تمیز:** استفاده انحصاری از فونت «استعداد» برای تمامی متن‌ها و دکمه‌ها.
4. **رعایت اصول واکنش‌گرایی:** نمایش کارت به صورت تمام‌عرض (Full-width) در صفحه‌نمایش‌های کوچک موبایل و عرض محدود در دسکتاپ.
